description: >-
  Markdown writing and technical documentation tools with linting,
  spell checking, presentation tools, and static site generators

depends_on:
  - components/core-development

homebrew:
  packages:
    - productivity  # includes pandoc

npm:
  packages:
    - markdown

vscode:
  packages:
    - markdown