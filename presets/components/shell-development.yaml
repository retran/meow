description: >-
  Shell development tools for scripting, task running, and documentation

depends_on:
  - components/shell-essential
  - components/node
  - components/pipx

homebrew:
  packages:
    - shell-development

apt:
  packages:
    - shell-development

apk:
  packages:
    - shell-development

pipx:
  packages:
    - shell-development

npm:
  packages:
    - shell-development

vscode:
  packages:
    - shell-development
