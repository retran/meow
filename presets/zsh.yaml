description: "Zsh shell and plugins"

depends_on:
  - core-shell
  - starship
  - zsh-autosuggestions
  - zsh-syntax-highlighting

homebrew:
  packages:
    - zsh

symlinks:
  - zsh

script: setup-zsh.sh
